#include "auraStats.h"

AuraStats::AuraStats(int spellPower, int naturePower, int firePower, int hasteRating, double hastePercent, double manaCostModifier)
    : spellPower(spellPower), naturePower(naturePower), firePower(firePower), hasteRating(hasteRating), hastePercent(hastePercent), manaCostModifier(manaCostModifier)
    {}